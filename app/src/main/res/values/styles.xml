<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="circular_image_view">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

    <style name="WindowAnimationTransition">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>

    <style name="BottomNavigationItemStyle" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="android:background">@drawable/nav_custom_ripple</item>
    </style>

    <style name="CustomAlertDialog" parent="ThemeOverlay.Material3.MaterialAlertDialog">
        <item name="shapeAppearanceOverlay">@style/CustomShapeAppearance</item>
    </style>

    <style name="CustomShapeAppearance" parent="ShapeAppearance.Material3.MediumComponent">
        <item name="cornerSize">10dp</item> <!-- Your desired corner radius -->
    </style>

    <style name="Widget.App.CheckBox" parent="Widget.Material3.CompoundButton.CheckBox">
        <item name="materialThemeOverlay">@style/ThemeOverlay.App.CheckBox</item>
    </style>

    <style name="ThemeOverlay.App.CheckBox" parent="">
        <!-- Container colors -->
        <item name="colorOnSurface">@color/bz_darkGray</item>
        <item name="colorPrimary">@color/bz_darkGray</item>
        <item name="colorError">@color/bz_destructive_red</item>
        <!-- Icon colors -->
        <item name="colorSurface">@color/bz_honeyYellow</item>
        <item name="colorOnPrimary">@color/bz_softGray</item>
        <item name="colorOnError">@color/bz_softGray</item>
    </style>
</resources>